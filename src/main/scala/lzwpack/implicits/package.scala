package lzwpack

/**
  * Provides a package `implicit` that, when imported, brings all the implicit conversions
  * used in the application into current scope. The implicits in this package are generally
  * commonly used extension methods to existing types or typeclass instances for existing types.
  */
package object implicits extends AllImplicits
